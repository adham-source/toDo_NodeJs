{{>header}}
    {{>navigation}}
    <section class="content_all_task my-3">
        <div class="container">
            {{#if status}}
            <h5 class="mt-3 text-uppercase">{{myTask.title}}</h5>
            <div class="row my-2 py-2">
                
                <div class="col-lg-4">
                    <div class="card text-white bg-dark mb-3" style="">
                        <div class="card-header">ToDo{{!--<a href="/editTask/{{myTask.id}}" class="alert-link">Edit this task</a>--}}</div>
                        <div class="card-body">
                            <h5 class="card-title">{{myTask.title}}</h5>
                            <p class="card-text">{{myTask.content}}</p>
                        </div>
                        {{!-- <div class="actions_btns card-body">
                            <a href="/singleTask/{{this.id}}"class="btn btn-success" >Show</a>
                            <button class="btn btn-danger">Delete</button>
                            <button class="btn btn-info">Edit</button>
                        </div>  --}}
                    </div>
                </div>
                
            </div>
            {{else}}
            <div class="alert alert-warning alert-dismissible fade show" role="alert">
                <strong>Not founded!</strong> You should checked .
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            {{/if}}
        </div>
    </section>
{{>footer}}